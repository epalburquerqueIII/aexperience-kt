<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epalburquerqueiii.aexperience">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.Newsletters.NewsletterActivity" />
        <activity android:name=".UI.Horarios.HorarioActivity" />
        <activity android:name=".UI.Autorizados.AutorizadoActivity" />
        <activity android:name=".UI.ConsumoBonos.ConsumoBonoActivity" />
        <activity android:name=".UI.Espacios.EspacioActivity" />
        <activity android:name=".UI.Menus.MenuActivity" />
        <activity android:name=".UI.Pagos.PagoActivity" />
        <activity android:name=".UI.Reservas.ReservaActivity" />
        <activity android:name=".UI.Tiposeventos.TiposEventoActivity" />
        <activity android:name=".UI.Tipospagos.TipospagoActivity" />
        <activity android:name=".UI.Usuarios.UsuarioActivity" />
        <activity android:name=".UI.Usuariosroles.UsuariorolActivity" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>